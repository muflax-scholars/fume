#!/usr/bin/env ruby

# load library
file = File.symlink?(__FILE__) ? File.readlink(__FILE__) : __FILE__
lib = File.join File.dirname(file), "/../lib/fume"

if File.exists? lib
  puts "using local version at #{lib}..."
  require lib
else
  require 'fume'
end

fume = Fume::CLI.new 10..20
fume.run
